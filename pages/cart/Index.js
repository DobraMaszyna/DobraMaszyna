import styled from 'styled-components';

const Cart = () => {
  return <CartStyled></CartStyled>;
};

const CartStyled = styled.div`
  display: flex;
  flex-direction: column;
`;

export default Cart;
